apiVersion: 1
datasources:
  - name: Timestream
    type: grafana-timestream-datasource
    access: proxy
    isDefault: true
    uid: nn_idXA4z
    jsonData:
      authType: keys
      defaultRegion: ${REGION}
    secureJsonData:
      accessKey: ${ACCESS_KEY}
      secretKey: ${SECRET_KEY}
    version: 1
    editable: true